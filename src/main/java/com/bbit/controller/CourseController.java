package com.bbit.controller;

import com.bbit.NotFoundException;
/*import com.bbit.model.course;*/
import com.bbit.repository.CourseRepository;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


/*@RestController
@RequestMapping(value = "courses")*/
/*public class coursecontoller{

    private final courseRepository courseRepository;
    public coursecontoller(courseRepository courseRepository){
        this.courseRepository=courseRepository:
    }
    @GetMapping("id/add/{someValues}")
    private course add(@PathVariable long id:
    @PathVariable int someValues){
        course course=courseRepository.findbyid(id)
                orElse Throw(()
                new NotFoundException()
    }*/




public class CourseController {
}
